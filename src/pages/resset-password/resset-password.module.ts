import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';

import { RessetPasswordPage } from './resset-password';
@NgModule({
  declarations: [
    RessetPasswordPage,
  ],
  imports: [
    IonicPageModule.forChild(RessetPasswordPage),
  ],
})
export class RessetPasswordPageModule {}
